val select_diffs :
    (Context_change.t list *
       (int * string (*path*) * string (*file*) * string (*region*))) list  ->
	 (Context_change.t list *
	    (int * string (*path*) * string (*file*) * string (*region*))) list
